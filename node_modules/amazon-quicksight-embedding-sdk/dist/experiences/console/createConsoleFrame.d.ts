import { ControlOptions, ConsoleContentOptions, ConsoleFrame, FrameOptions } from '../../types';
declare const createConsoleFrame: (frameOptions: FrameOptions, contentOptions: ConsoleContentOptions, controlOptions: ControlOptions, allExperienceIdentifiers: Set<string>) => ConsoleFrame;
export default createConsoleFrame;
